# MG4-Dashcam-Trigger
MG4 Dashcam Trigger is a lightweight sidebar launcher app that sends an external trigger to the MG4-360-Camera-App, causing it to save the current dashcam event instantly without opening the main app screen.
